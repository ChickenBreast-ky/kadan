# 흉터 목록 — 카단 스킬의 규칙이 생긴 사고들

## Why

규칙 옆에 사연을 다 적으면 스킬이 무거워져 매번 읽는 사람이 놓친다. 스킬 본문에는
규칙과 `(사고 MM-DD)` 꼬리표만 두고, 사연은 여기 한 줄씩 모은다 (2026-09-02 card-42).
kadan-conductor·kadan-super·kadan-secretary가 같이 쓴다. 코드로 잠긴 사고는 시험
이름(`tests/*.test.mjs`)에도 같은 날짜가 붙어 있다.

원문: 카단 이전 시절의 사고 기록은 관리자 개인 스킬 저장소에, 카단 시절은
작업장 기록 레포 `docs/daily/<날짜>/`에 있다.

## 목록

| 날짜 | 사고 한 줄 | 생긴 규칙 |
|---|---|---|
| 07-13 | 코드 배선과 단위 테스트만 보고 완료 처리했는데 실제 화면은 안 나왔다 | 사용자 눈에 보이는 작업은 실물 증거 1개 (card-template) |
| 07-19 | 검수자의 서브에이전트가 공유 worktree를 수정했다 | 서브에이전트에 읽기 전용 계약 전파 (card-template) |
| 07-19 | 방치된 검수 세션이 구현자와 같은 파일을 계속 만졌다 | 검수 종결 즉시 검수 역할 stop |
| 07-22 | "마스킹을 빼서 실패를 보라"는 검수 카드에 검수자가 막혀 표류했다 | 검수 카드에 소스 수정 요구 금지 |
| 07-23 | 한 작업자에게 지휘자 둘이 붙어 이중 개입 | 이상 하나에 수신자 하나, 비서는 작업자에게 직접 안 보냄 |
| 07-23 | 429로 죽은 실행기에 그대로 재발령해 즉사 (두 번) | 한도는 같은 역할 재개 금지 (watch `한도` 낱말) |
| 07-24 | 미커밋 누적분이 섞여 라운드별 diff 범위를 잃었다 | 검수 합격 단위를 지휘자가 즉시 체크포인트 커밋 |
| 07-27 | 검수 근거 파일이 worktree 안에 생겨 diff를 오염시켰다 | 근거 파일은 결과 파일 경로에만 (card-template) |
| 07-28 | "X면 중단·보고"만 준 지시가 X가 나자 조용한 대기를 만들었다 | 예상 분기의 기본 처리를 지시에 같이 박는다 |
| 07-28 | 슈퍼가 화면 조각을 재구성해 "검수 진행 중"으로 오보 | 상태는 원장·화면·PID 원문으로만 |
| 08-03 | 결정 관문 편지를 읽음 처리한 것을 해결로 세었다 | 읽음은 전달 증거일 뿐, 답을 받아야 해결 |
| 08-04 | 후임 화면만 뜨고 등록이 안 됐는데 교대 판정될 뻔 | 교대 순서 고정, 후임이 실제 일 1회 뒤 선임 정지 (handover) |
| 08-05 | 정상 장고(도구 없이 생각 중)를 정체로 오탐 | 무진행 판정 순서 3단계, 정상 장고엔 개입 안 함 (intervention) |
| 08-06 | 429를 "정상 대기"로 봐서 다섯 시간 정체 | 화면 하단 오류 원문부터 읽는다 |
| 08-06 | 502·프록시 끊김을 판 실패로 보고 접었다 | 끊김은 상시 조건, 판을 접지 않고 같은 역할 재개 |
| 08-07 | 감독이 1시간 30분 동결됐는데 중계기 일기에만 적혔다 | 판정≠알림≠조치 네 단계 확인, 감독 idle은 슈퍼에게 상신 (코드) |
| 08-07 | 새 저장소의 승인 창에 감독·중계기가 막혔고 사람이 대신 눌렀다 | 발령 전 `kadan read`로 신뢰 창 확인, 자동 통과 금지 |
| 08-07 | 중계기가 요청당 25,413토큰을 먹었다 | judge는 하단 60줄·3,000자만 (코드) |
| 08-09 | 스왑 22.5GB로 기계가 죽었다 | 자원은 메모리→스왑→CPU 순서, 25%/15% 관문 (코드) |
| 08-09 | 감독이 11시간 정지했는데 아무도 깨우지 않았다 | 미완료 카드 + 감독 화면 무변화 30분이면 슈퍼에게 (코드) |
| 08-10 | 슈퍼가 한 번 읽은 80%로 교대를 올렸다가 취소 | 한 번 읽은 %는 근거가 아니다 (handover) |
| 08-10 | "표준에 박았습니다"만 여덟 번, 어디인지 아무도 몰랐다 | 기록 보고는 `파일:줄 — 누가 언제 읽는가` |
| 08-10 | 터미널 한 줄만 남기고 끝나 보고가 유실됐다 | 완료 보고 3종: 결과 파일·DONE 마커·터미널 한 줄 |
| 08-10 | 구현 문맥을 이어받은 검수자가 결함 2개를 놓쳤다 (EXP-RI-1) | 검수자는 새 세션, 2~3분 합격은 덜 판 것부터 의심 |
| 08-11 | 메모리 경고가 오탐이었다 | 25%/15% 숫자를 사연과 함께 시험에 잠금 (코드) |
| 08-30 | 지문에 넣은 완성 마커가 입력 에코로 읽혀 완료 오판 | 지문엔 마커 형식과 카드 id만 (코드) |
| 08-30 | 창 폴백 체인이 Ghostty를 엉뚱하게 열었다 | 창은 기계당 하나, 폴백 없음 (코드) |
| 08-31 | 다른 판 역할까지 종료할 뻔했다 (`mc-*`만 종료한 것이 옳았다) | 내 판 역할만 하나씩 stop |
| 08-31 | 비서가 코드를 고치자 시험 55개가 통과해도 판정 근거가 반대로 돌았다 | 비서는 제품 코드를 고치지 않는다 |
| 08-31 | 문서 3줄 고치는 데 슈퍼감독이 전체 관문 시험을 돌렸다 | 카드로 발령 안 된 문서·규칙은 비서가 직접 고친다 |
| 08-31 | "계속 진행해"를 설계 변경 승인으로 읽었다 (card-26) | 승인 범위를 넓게 읽지 않는다 |
| 09-01 | 격리 실측 명령 하나가 env를 빠뜨려 실제 원장에 6줄을 남겼다 (card-34) | export는 셸 맨 위에서 한 번, 전후 원장 줄 수 대조. 코드로 막지 않는다 |
| 09-01 | 판 3개가 카드 6건을 확정 없이 접어 미아가 됐다 | 종료 전 `kadan done`, `stop`이 미확정 카드를 알림 (코드) |
| 09-01 | card-34가 70/70인데 절차 위반 | 절차 위반은 기능이 돼도 failed |
| 09-01 | 판 두 개 병렬로 CPU 5분 평균 25.57 (12코어) | 자원 AMBER·RED면 새 판을 열지 않는다 |
| 09-01 | 비서가 감독의 73%를 한 번 읽고 교대를 걱정 | 08-10과 같은 실수 (handover) |
| 09-02 | 08-30 스킬 초판이 어댑터 금지를 라우터 금지로 넓혀 적었다 | 라우터·자동 교체는 금지가 아니라 아직 없는 것 |
