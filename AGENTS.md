# kadan — 이 저장소에서 코드를 고치는 에이전트에게

이 파일은 카단 **코드를 수정하는** AI를 위한 최소 규칙이다. 카단을 *사용하는* 감독·비서·작업자 역할은 이 파일이 아니라 `skills/`를 읽는다.

1. 의존성 제로. Node 내장 + tmux + OS 기본 명령만. 새 의존성은 관리자 결정 전에 넣지 않는다.
2. 원장은 append-only. 사건을 수정·삭제·재정렬하는 코드를 만들지 않는다. 저장은 `src/storage.mjs`를 통한다.
3. 실행기(codex·claude 등)별 어댑터·라우터를 만들지 않는다. 필요하면 먼저 이슈로.
4. `send` 전 생존 확인(세션 존재 + pane PID 일치)을 우회하지 않는다. 원인 모르는 실패는 재시도하지 않는다.
5. 작업 지문에 완성된 `KADAN:DONE` 마커를 쓰지 않는다. 형식과 카드 id만 준다.
6. 동작 변경은 시험과 같은 커밋으로. `npm test`, `npm run check:public` 통과 후 커밋한다.
7. `git add -A` / `git add .` 금지. 파일을 지정해 추가한다.
8. `docs/daily/`는 이 저장소의 일부가 아니다(관리자 기계에서는 별도 비공개 레포가 중첩됨). 거기 있는 파일을 커밋하거나 참조 경로로 쓰지 않는다.

설계 배경·잠긴 규칙의 이유·원장 기록 형식·용어는 [docs/design.md](docs/design.md), 기여 절차는 [CONTRIBUTING.md](CONTRIBUTING.md)를 본다.
